{
  "plugins": {
    "KeywordDetector": {
      "keyword_exclude": null,
      "keyword_include": [
        "password", "passwd", "pwd", "pass", "secret", "token", "auth", "key", "credential", "api_key", "access_token", "nextcloud_user", "nextcloud_pass", "nextcloud_url"
      ]
    }
  }
}
